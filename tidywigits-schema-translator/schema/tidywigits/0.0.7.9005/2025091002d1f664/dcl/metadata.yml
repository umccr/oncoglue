version: 2
models:
- name: sat_metadata
  columns:
  - name: library_workflow_run_hk
    data_type: varchar(64)
  - name: load_date
    data_type: date
  - name: hash_diff
    data_type: varchar(64)
  - name: record_source
    data_type: varchar(255)
  - name: last_seen_datetime
    data_type: timestamp
  - name: batch_id
    data_type: varchar(26)
  - name: batch_date
    data_type: date
  - name: input_dirs
    data_type: array<varchar>
  - name: output_dir
    data_type: varchar
  - name: pkg_versions
    data_type: 'array<struct<name: varchar, version: varchar>>'
  - name: files
    data_type: 'array<struct<tbl: varchar, prefix: varchar, fout: varchar, fin: varchar>>'
