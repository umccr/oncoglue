models:
  - name: cobalt_gcmed_buckets
    columns:
      - name: input_id
        data_type: varchar
      - name: input_prefix
        data_type: varchar
      - name: output_id
        data_type: varchar
      - name: gc_bucket
        data_type: varchar
      - name: median
        data_type: double
