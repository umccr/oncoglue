models:
- name: chord_signatures
  columns:
  - name: input_id
    data_type: varchar
  - name: input_prefix
    data_type: varchar
  - name: output_id
    data_type: varchar
  - name: signature
    data_type: varchar
  - name: count
    data_type: bigint
