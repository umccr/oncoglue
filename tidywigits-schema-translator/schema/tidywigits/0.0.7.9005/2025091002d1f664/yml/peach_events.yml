models:
- name: peach_events
  columns:
  - name: input_id
    data_type: varchar
  - name: input_prefix
    data_type: varchar
  - name: output_id
    data_type: varchar
  - name: event
    data_type: varchar
  - name: count
    data_type: double
